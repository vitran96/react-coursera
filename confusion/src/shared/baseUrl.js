const basePort = 6060;
export const baseUrl = `http://localhost:${basePort}`;
export const dishesUrl = `${baseUrl}/dishes`;
export const commentsUrl = `${baseUrl}/comments`;
export const promotionsUrl = `${baseUrl}/promotions`;
